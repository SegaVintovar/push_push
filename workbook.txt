alorithm ideas

divide into steps
1.sort the tmp_stack to understand the indexes of the iteams in sorted way
2.push b all the numbers with indexes that " i % 2 != 0 " in acending(growing) order.
3.get the biggest number to the top of the stack_a(via rotate) and push a. because we have b sorted.
4.get third biggest value on top stack_a and push it b. rotate stack_a so the second biggest iteam is
	on top and push a two times, because there were two sorted elements in row
5. do the same with fifth element

next stage
i need stack_a also to be sorted in the growing way, 

presort?
look for sequences of numbers. how?
account them

RULE stack A in ascending order
		stack B in descending

new ideas
phase 1
		1. always check if top element and next one are in the right order
			if not swap.
		2. create new arr with 0 and 1 that are representing if the other elements are in the right order
			this arr has to be updated with every stack a operation at this phase
		3. push b if they are not
phase 2
		1. cost calculation for every element in stack_b to place it in right place in stack a
			HOW?
				1. position in stack b (first half or second half)
					if first then  
		2. pa to the correct location
2. if they are in the right order